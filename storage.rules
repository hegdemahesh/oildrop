rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /invoices/{file} {
      allow read: if request.auth != null; // refine to role check
      allow write: if request.auth != null; // Cloud Functions preferred for writes
    }
  }
}
